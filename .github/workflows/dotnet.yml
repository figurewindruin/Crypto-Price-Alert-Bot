name: .NET CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore Crypto-Price-Alert-Bot.sln
      - name: Build
        run: dotnet build Crypto-Price-Alert-Bot.sln --no-restore
      - name: Test
        run: dotnet test Crypto-Price-Alert-Bot.sln --no-build --verbosity normal
